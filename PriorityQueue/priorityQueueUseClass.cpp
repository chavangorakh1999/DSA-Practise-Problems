#include <bits/stdc++.h>
#include "./priorityQueue.h"
using namespace std;
int main()
{
    PriorityQueue q;
    q.insert(10);
    q.insert(30);
    q.insert(50);
    q.insert(2);
    q.display();
    return 0;
}